- compact ||= false
- compact_header ||= false

.card
  .card-header
    - if compact_header
      | Stand
    - else
      = link_to competition_path(competition)
        = "#{competition.competitiesoort.humanize} - #{competition.klasse}"
      span.lighter.pl-2
        = "#{competition.poule}"
      .light.small
        = "#{competition.competitienaam}"

  .card-body.p-0
    - if competition.ranking
      table.table.table-sm.tr-links
        thead
          tr
            th
            th Team
            th.text-center[title="Gespeeld"]
              | G
            - unless compact
              th.text-center[title="Gewonnen"]
                | W
              th.text-center[title="Gelijk"]
                | GL
              th.text-center[title="Verloren"]
                | V
            th.text-center[title="Punten"]
              | P
            - unless compact
              th.text-center[title="Doelpunten voor"]
                | DPV
              th.text-center[title="Doelpunten tegen"]
                | DPT
            th.text-center[title="Doelsaldo"]
              | DS
          tbody
            - competition.ranking.each do |line|
              tr[class="#{'strong table-success' if line['eigenteam'] == 'true'}"]
                td.text-right
                  = line['positie']
                td
                  = line['teamnaam']
                td.text-center
                  = line['gespeeldewedstrijden']
                - unless compact
                  td.text-center
                    = line['gewonnen']
                  td.text-center
                    = line['gelijk']
                  td.text-center
                    = line['verloren']
                td.text-center
                  = line['punten']
                - unless compact
                  td.text-center
                    = line['doelpuntenvoor']
                  td.text-center
                    = line['doelpuntentegen']
                td.text-center
                  = line['doelsaldo']
    - else
      .p-3
        i Geen stand gevonden

.row
  .col-sm-6

  .col-sm-6
