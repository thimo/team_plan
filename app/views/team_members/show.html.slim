- provide(:title, "#{@team_member.member.name}")

.box-typical.box-typical-padding
  = render "shared/member", member: @team_member.member
        
  - if policy(@team_member).show_comments?
    .row
      .col-lg-12
        = render "comments/tabs"
